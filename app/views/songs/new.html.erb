<h1>Create a Song</h1>

<%= form_for @song  do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <br />
  <%= f.label 'artist name' %>
  <%= f.text_field :artist_name %>
  <br />
  <%= f.label 'select' %>
  <%= f.collection_select :genre_id, Genre.all, :id, :name, prompt: true %>
  <br />
  <input name="song[note_contents][]" id="song_notes_1">
  <input name="song[note_contents][]" id="song_notes_2">
  <!-- Song.first.notes => gives me instances of each note contents. -->
  <!-- Song.first.note_contents => gives me array of just note contents. -->
  <!-- using pry,self.notes.count => 2. in note_contents method. Is this why I have two of these?
https://guides.rubyonrails.org/v3.2.13/form_helpers.html#understanding-parameter-naming-conventions-->

  <%= f.submit %>
<% end %>
